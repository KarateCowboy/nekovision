use yew::prelude::*;
use crate::update::State;

#[function_component]
pub fn View(state: &State) -> Html {
    html! {
        <main class="container">
           <ul>
           </ul>
        </main>
    }
}